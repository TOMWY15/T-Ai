install python and execute this code on CMD:
1. pip install torch numpy
2. pip install bash
3. pip install flask (if not installed yet)
4. pip install pygame (in chase you need to code a game with him)
5. have fun
